[WebService]
{%if "master" in group_names %}
LoginTitle = "Galaxy Kubernetes master {{inventory_hostname}}"
{%elif "worker" in group_names %}
LoginTitle = "Galaxy Kubernetes worker {{inventory_hostname}}"
{%else%}
LoginTitle = "Galaxy member (No Kubernetes)"
{%endif%}