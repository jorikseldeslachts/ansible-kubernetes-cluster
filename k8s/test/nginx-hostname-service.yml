apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx-hostname
  name: nginx-hostname-service
  namespace: nginx-hostname
spec:
  ports:
  - nodePort: 30500
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: nginx-hostname
  type: NodePort